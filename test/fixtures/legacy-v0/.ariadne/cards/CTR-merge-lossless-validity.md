# CTR-merge-lossless-validity: Lossless divergence versus single valid graph

- Status: RESOLVED
- Provenance: FACT
- Type: CTR
- Revised: 2026-08-30

## Statement

Both branch models must remain available while the active Epistemic Graph permits one materialized revision per node ID and requires a valid deductive DAG.

## Payload

```json
{
  "conflicting_properties": [
    "retain both incompatible branch variants",
    "materialize one structurally valid active graph"
  ],
  "resolution": "Use graph-native content-addressed Merge Contradictions and Causal Quarantine to preserve both branch variants while keeping the active graph valid."
}
```
