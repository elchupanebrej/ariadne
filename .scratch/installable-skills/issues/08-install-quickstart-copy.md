# Fix the user-facing installation story

Type: grilling
Blocked by: 01, 04, 05, 06, 07

## Question

The map's final decision: the exact copy users see on both channels.

1. README quickstart: which one-two commands we print for each channel (skills.sh and npm), in what order.
2. What the skill page on skills.sh shows: description, source, link.
3. How the user updates (`npx skills update`, `npm update` / `npx <pkg>@latest`) and what we say about it.
4. The skill's message when the CLI is missing (if the `install`-contract ticket left it to this one).

Once resolved, the way to the destination is clear: only the subcommand implementation and pasting the copy remain.
