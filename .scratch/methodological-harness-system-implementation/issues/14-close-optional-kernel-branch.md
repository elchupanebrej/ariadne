# 14 — Close the optional Orchestration Kernel branch

**What to build:** Apply the matched thin-path evidence non-compensatorily: retain no kernel when the thin path satisfies every hard continuation invariant, or implement the smallest co-located owner-neutral kernel that repairs the specifically falsified invariants and rerun the same evidence cases.

**Blocked by:** 08 — Verify Harness Authoring transfer and deletion discipline; 13 — Measure effect safety on the thin path.

**Status:** ready-for-agent

- [ ] The disposition lists every hard invariant, its thin-path evidence, and its supported, falsified, or inconclusive verdict without weighted averaging.
- [ ] If the thin path passes, no kernel code or state remains and an executable deletion or absence check protects that result.
- [ ] If retention is justified, one co-located internal TypeScript module owns only pointer-based attempt lifecycle state and remains separate from Ariadne's controller and Epistemic Overlay.
- [ ] A retained kernel uses one append ledger per attempt, serializes only that attempt, validates expected revision, and persists Dispatch Intent before invocation.
- [ ] Recovery accepts at most an incomplete final record, preserves immutable history, and routes existing intent to owner inspection.
- [ ] The retained path passes the exact matched cases that falsified every eligible thinner candidate.
- [ ] Claims stay bounded to the tested local process and same-volume filesystem; power-loss and distributed-concurrency safety remain unclaimed.
- [ ] No new package, service, database, queue, scheduler, IPC protocol, global lock, migration framework, rollback engine, compensation engine, or alert router is introduced without separate evidence.
