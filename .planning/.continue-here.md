---
context: default
phase: production-readiness-task-22
task: 22
total_tasks: 25
status: in_progress
last_updated: 2026-09-09T10:18:24.717Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failures. A resuming agent must acknowledge them before continuing.

- [ ] CONSTRAINT: broken npm/node shim — `/mnt/c/Users/bulky/node_modules/.bin/node` contains the literal text `This file intentionally left blank`; direct npm scripts can fail before the repository code runs. Use an installed absolute Node 22/24 binary for verification until the shim is repaired.
- [ ] CONSTRAINT: single-run benchmark evidence is not a new contract — Node 24 exceeded the RSS ceiling and Node 22 exceeded the `open` p95 budget on repeat evidence. Do not edit thresholds from one run; first collect repeated, clean supported-matrix measurements.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Treating test timeout as product SLO | The CLI suite needed `--testTimeout 60000`; earlier 15-second failures were test-duration timeouts, not CLI assertion failures. | advisory | Keep the 60-second Vitest timeout explicitly labeled as test infrastructure and report product benchmark budgets separately. |
| Raising a release gate from one machine/run | The latest evidence is mixed across Node 22 and Node 24, and RSS is still over the approved ceiling on Node 24. | blocking | Require repeated seeded runs on clean supported Linux runners and record p50/p95, RSS validity, capacities, runtime, and pass/fail before changing the contract. |

<current_state>

There is no active `.planning/phases/*/PLAN.md`, so the pause workflow selected the default handoff location. The current tracker position is production-readiness task 22, “Align CLI Grammar and Diagnostic Exit Semantics”. It is already resolved:

- Implementation: `3cc54c7`.
- Tracker/map resolution: `36654e0`.
- Node 24 direct Vitest verification: `tests/cli` passed 16 files and 202 tests with `--testTimeout 60000`; `merge.test.ts` passed 14/14; `merge-integration.test.ts` passed 16/16.

The request for “explicit measured limits” crosses into task 23. The approved capacity/performance contract currently is:

| Class / resource | Approved limit | Latest relevant evidence |
|---|---:|---|
| Fast query p95 | `< 100 ms` | Node 24 ceiling run passed all latency metrics after indexing/cache work. |
| Standard operation p95 | `< 1,000 ms` | Node 22 repeat `open` p95: `1,043.671 ms` — fail. |
| Batch operation p95 | `< 10,000 ms` | Node 24 ceiling migration p95: `9,594.891 ms` — pass, with little headroom. |
| Attributable RSS | `<= 256 MiB` (`268,435,456` bytes) | Node 24: `272.46 MiB` — fail; Node 22 memory check passed. |
| Ceiling graph | `10,000` nodes / `25,000` edges / `50,000` events | Exact capacities exercised. |
| Cards / reports / concurrency | `10,000` cards / `50` reports / `2` processes | Exact declared capacities and two-process concurrency exercised. |

These are release-gate limits from tasks 09/14/23, not task-22 CLI limits. No threshold was changed in this handoff. Increasing them may be reasonable, but it requires a new evidence-backed decision because task 23 is still `claimed` and task 24 depends on it.

</current_state>

<completed_work>

Completed Tasks:

- Task 22: CLI grammar, help/version output, diagnostics, and exit classes implemented and verified — done (`3cc54c7`).
- Task 22 tracker finalization: issue status and map pointer recorded as resolved — done (`36654e0`).
- Capacity evidence follow-up: benchmark tooling and performance optimizations recorded; supported-runtime results added to task 23 — done as evidence collection (`6621c70`).

</completed_work>

<remaining_work>

- Task 23: finish reproducible capacity/performance evidence on clean Linux Node 22 and Node 24 runners; then decide whether measured limits should be rebaselined. Keep the current limits until that decision is recorded.
- Task 24: packaging/release gates remain blocked by task 23.
- Task 25: verification/worktree curation remains blocked downstream.
- Resolve or explicitly preserve the unrelated uncommitted package, mode-D test, benchmark JSON, and diagram-generation artifacts; do not include them in the handoff WIP commit.

</remaining_work>

<decisions_made>

- Task 22 is complete; capacity work belongs to task 23 and must not be disguised as a CLI-contract change.
- The current thresholds remain the approved baseline: fast `<100 ms`, standard `<1 s`, batch `<10 s`, attributable RSS `<=256 MiB`, and the declared 10K/25K/50K/10K/50/2 capacity envelope.
- A possible increase is a proposal only. Derive any revised values from repeated p95 and RSS measurements plus an explicit tracker/spec answer; do not infer them from the 60-second test timeout.
- Node 22.23.0 and Node 24.17.0 are installed. Use their absolute binaries while the external npm/node shim remains broken.

</decisions_made>

<blockers>

- Task 23 is not release-ready: Node 24 is over the 256 MiB RSS ceiling; Node 22 has a repeat `open` p95 over the 1-second standard budget.
- Normal npm lifecycle commands are unreliable in this worktree because `/mnt/c/Users/bulky/node_modules/.bin/node` is invalid. `npm ci --ignore-scripts` succeeded; use direct Node commands for tests and benchmarks.
- Historical comments in task 20 still describe an earlier unresolved state even though its top-level status and answer say `resolved` and implementation is in `a475a60`. Treat the current status/answer as authoritative, but avoid rewriting that unrelated tracker history during this handoff.

</blockers>

## Required Reading (in order)

1. `.scratch/production-readiness/issues/22-cli-contract-and-exit-semantics.md` — task-22 scope, resolved answer, and CLI evidence.
2. `.scratch/production-readiness/issues/23-capacity-performance-ci-gates.md` — open capacity gate and latest supported-runtime evidence.
3. `.scratch/production-readiness/issues/09-decide-capacity-and-performance.md` — approved capacity envelope and latency/memory contract.
4. `.scratch/production-readiness/spec.md` sections 8–9 — normative limits and release-gate placement.
5. `tests/benchmarks/generator.ts` and `tests/benchmarks/runner.ts` — benchmark tiers, thresholds, and pass/fail implementation.
6. `scripts/run-benchmark.mjs` — benchmark CLI and structured evidence output.

## Critical Anti-Patterns (do NOT repeat these)

- Do not run the benchmark through `npx` or a normal npm lifecycle command until the external node shim is repaired; invoke the installed Node binary and repository entry point directly.
- Do not call task 22 incomplete merely because the CLI tests need a 60-second test timeout.
- Do not increase the RSS or latency gates before reproducing the measurements on clean supported runners and recording the rationale in task 23/spec artifacts.

## Infrastructure State

- Branch: `codex/first-version`.
- Installed runtimes: Node `v22.23.0` / npm `10.9.8`; Node `v24.17.0` / npm `11.13.0`.
- External shim failure: `/mnt/c/Users/bulky/node_modules/.bin/node` is not an executable Node binary.
- No background server, watcher, or async external job is running as part of this handoff.
- The handoff commit must include only `.planning/HANDOFF.json` and `.planning/.continue-here.md`; preserve all existing user changes.

## Pre-Execution Critique Required

Not applicable. This handoff pauses after implementation and evidence collection; no design artifact is awaiting execution.

<context>

The important correction for the next session is ownership: task 22 is resolved, while the explicit measured limits are task-23 evidence. The user suspects the limits can be increased. Test that hypothesis with repeated measurements and a documented decision. The current Node 24 result gives a strong latency signal but fails RSS; the Node 22 result gives a near-threshold standard latency signal. This is enough to plan a rebaseline, not enough to silently change release gates.

</context>

<next_action>

Start with a clean repeated ceiling benchmark on Linux Node 22 and Node 24 using seed 42. Record all p95 metrics, RSS validity/attribution, exact capacities, runtime, and total duration. Then compare the observed distributions with the current limits before proposing any increase in task 23.

</next_action>
